.. _module-gertz:

gertz
*****

This module exports no functions. It inserts a pre-hook into the message processing pipeline as part of an old inside joke. A hook which is disabled by default anyway, because there are probably half a dozen people in the world who remember what it was about and find it amusing.

